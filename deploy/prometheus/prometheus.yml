global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: taskdeck-core
    metrics_path: /metrics
    static_configs:
      - targets: ["core:8000"]
        labels:
          service: core
